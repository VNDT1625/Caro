AI service (Python)

Suggested: FastAPI microservice serving endpoints for:
- move suggestion (AI engine)
- post-match analysis (replay analyzer)

Run with Uvicorn for dev:

```powershell
uvicorn ai.api.main:app --reload --port 8001
```
