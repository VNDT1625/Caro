## Verification Notes

- Dữ liệu mẫu: `ai/test_data/games/*`, `ai/test_data/edge_cases/*`, `ai/test_data/patterns/*`.
- Chạy script: `python ai/test_custom_game.py --all` để so sánh kết quả phân tích với expected trong file.
- Độ chính xác mục tiêu: >=85% (tính thô trên tỷ lệ move OKAY+).
- Cập nhật mismatches vào đây sau mỗi lần chạy để tinh chỉnh tham số nếu cần.
