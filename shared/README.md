Shared artifacts

Place shared types, protobufs, validation rules or game logic ports here so client/server/ai keep the same contract.

Example content:
- `shared/types/` for TS/PHP interoperable schemas
- `shared/proto/` for gRPC if used
