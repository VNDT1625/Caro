# Vite + Supabase env example
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key

# Storage bucket cho avatar (tạo bucket này trong Supabase Storage, public hoặc policy cho GET)
VITE_SUPABASE_AVATAR_BUCKET=avatars

# API proxy (dev) - set to backend dev URL
VITE_API_URL=http://localhost:8000

# AI service (FastAPI) base URL + optional key
VITE_AI_URL=https://openrouter.ai/api/v1/chat/completions
VITE_AI_API_KEY=
VITE_AI_MODEL=tngtech/deepseek-r1t2-chimera:free

# Access gating (client-side guard; should be enforced server-side too)
# Regex to allow Trial (e.g., edu email). Leave empty to disable.
VITE_AI_TRIAL_EMAIL_REGEX=.*@.+\.edu(\.vn)?$
