<?php
// Placeholder bootstrap file. When using a framework (Laravel), this will be replaced.

// Load environment
if (file_exists(__DIR__ . '/../.env')) {
    // load .env if needed
}

// Return application/container if using one
return null;